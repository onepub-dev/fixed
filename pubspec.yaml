name: fixed
version: 6.2.0
homepage: https://github.com/onepub-dev/fixed
documentation: https://fixed.onepub.dev
description: Fixed scale decimal numbers with mathematical operations, formatting and parsing.
repository: https://github.com/onepub-dev/fixed
environment:
  sdk: '>=3.6.0 <4.0.0'
dependencies:
  decimal: ^3.0.2
  intl: ^0.20.3
  meta: ^1.9.0
dev_dependencies:
  lint_hard: ^7.1.2
  test: ^1.16.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
